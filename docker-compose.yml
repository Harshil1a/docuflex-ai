services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - docuflex_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: docuflex

volumes:
  docuflex_mongo_data:
