{
  "name": "docuflex-ai",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "npm run build -w frontend && npm run dev:api",
    "dev": "concurrently -k -n api,ui -c cyan,magenta \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "node scripts/run-backend.cjs",
    "dev:ui": "npm run dev -w frontend",
    "dev:ui-only": "npm run dev -w frontend",
    "db:up": "docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
